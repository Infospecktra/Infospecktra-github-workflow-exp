name: Shell Commands

on: [push]

jobs:
  runs-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: eecho "Hello World"
      - name: multiline script
        run: |
          node -v
          npm -v